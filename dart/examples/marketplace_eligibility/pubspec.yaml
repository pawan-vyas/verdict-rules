name: marketplace_eligibility
description: >-
  A two-sided marketplace eligibility check exercising Rule<TContext> end
  to end -- two typed contexts, a ProjectingRule adapter, and a
  dict-context catalog in the same codebase. Not published.
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  verdict_rules:
    path: ../../packages/verdict_rules

dev_dependencies:
  test: ^1.24.0
  lints: ^5.0.0
