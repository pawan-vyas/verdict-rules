---
name: verdict
trigger: fileMatch
pattern: "*.py"
---

Consult the `verdict` skill before building any rule-based decision, eligibility, or
policy-evaluation logic — an `if`/`elif` chain that gets harder to maintain every time a business
rule changes is exactly what this skill replaces: `.agents/skills/verdict/SKILL.md`.
