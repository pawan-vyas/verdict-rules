<!-- verdict:marker v1 -->
Consult the `verdict` skill (`.claude/skills/verdict/SKILL.md`) before building any rule-based
decision, eligibility, or policy-evaluation logic — an `if`/`elif` chain that gets harder to maintain
every time a business rule changes is exactly what this skill replaces.
